apiVersion: v1
kind: ConfigMap
metadata:
  name: my-config-map
  namespace: default
data:
  config.json:  |-
        {
          "hello_1": "$REPLACE_ME",
          "hello_2": "$$REPLACE_ME",
          "hello_3": "${REPLACE_ME}",
          "hello_4": "$${REPLACE_ME}",
        }
