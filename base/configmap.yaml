apiVersion: v1
kind: ConfigMap
metadata:
  name: my-config-map
  namespace: default
data:
  config.json:  |-
        {
          "hello_A": "$REPLACE_ME",
          "hello_B": "$$REPLACE_ME",
          "hello_C": "${REPLACE_ME}",
          "hello_D": "$${REPLACE_ME}",
        }
